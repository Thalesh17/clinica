<div class="modal fade" data-backdrop="static" id="{{ $idModal ? $idModal : 'modal-form' }}" role="dialog" aria-labelledby="myModalForm">
    <div class="modal-dialog {{ isset($typeModal) ? $typeModal : 'modal-md' }}" role="document">
        <div id="{{ $idContent ? $idContent : 'content-modal' }}" class="modal-content modal-width">
            
        </div>
    </div>
</div>